# VibeBalancer-Tasks
Contains all task descriptions for VibeBalancer, an IEEE Envision 2026 Project.

## Expo demo dashboard

For a live interactive demo, this repo includes a Grafana + Prometheus setup in `docker-compose.monitoring.yml`.

See `ENHANCED_DEMO.md` for the full run steps.
